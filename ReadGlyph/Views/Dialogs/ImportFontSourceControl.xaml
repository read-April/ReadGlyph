<UserControl x:Class="ReadGlyph.Views.Dialogs.ImportFontSourceControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Width="550" Height="300"
             d:DesignHeight="300" d:DesignWidth="550">
    <Border Style="{StaticResource CardBorder}" Padding="28,24">
        <StackPanel>
            <!-- 标题 -->
            <TextBlock Text="📂 导入字体源" FontSize="18" FontWeight="Medium" Margin="0,0,0,12">
                <Run Foreground="{StaticResource TextMutedBrush}" FontSize="13" FontWeight="Normal" Text=" 资源库" />
            </TextBlock>
            <Rectangle Height="1" Fill="{StaticResource BorderBrush}" Margin="0,0,0,18" />

            <!-- 字体显示名称 -->
            <StackPanel Margin="0,0,0,14">
                <TextBlock Text="字体显示名称" Style="{StaticResource LabelStyle}" />
                <TextBox x:Name="TxtDisplayName" Text="" />
            </StackPanel>

            <!-- 字体文件 -->
            <StackPanel Margin="0,0,0,18">
                <TextBlock Text="字体文件" Style="{StaticResource LabelStyle}">
                    <Run Foreground="{StaticResource AccentBrush}" Text=" *" />
                </TextBlock>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition Width="6" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" x:Name="TxtFontFile"
                             Text="" />
                    <Button Grid.Column="2" x:Name="BtnPickFontFile"
                            Style="{StaticResource FilePickerButton}"
                            ToolTip="从文件系统选择 .ttf / .otf / .ttc 字体文件"
                            Click="BtnPickFontFile_Click" />
                </Grid>
                <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                    <TextBlock Text="支持 " FontSize="11" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center"/>
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="2,0" Margin="2,0">
                        <TextBlock Text=".ttf" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="2,0" Margin="2,0">
                        <TextBlock Text=".otf" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="2,0" Margin="2,0">
                        <TextBlock Text=".ttc" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="2,0" Margin="2,0">
                        <TextBlock Text=".woff" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                    <Border Background="{StaticResource BackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" Padding="2,0" Margin="2,0">
                        <TextBlock Text=".pfb" FontFamily="Consolas" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </Border>
                </StackPanel>
            </StackPanel>

            <!-- 按钮 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                <Button Content="取消" Style="{StaticResource {x:Type Button}}" Margin="0,0,10,0" />
                <Button Content="导入" x:Name="BtnImport" Style="{StaticResource PrimaryButton}" />
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>